# SafeMeal 개발 작업 보고서

작성일: 2026-07-09

---

## 1. 왜 이 작업을 했는가

정의서(`관광 음식메뉴판 데이터 정제 및 검색 서비스 웹개발정의서`)의 목표는 두 가지였습니다.

1. AI Hub "관광 음식메뉴판 데이터" 원천 데이터에 존재하는 품질 결함을 **백엔드 단에서
   기술적으로 방어**할 것
2. 외부 LLM API·벡터DB 없이 **완전 오프라인**에서 구동되는 알레르기 회피/오역 교정
   검색 엔진을 만들 것

정의서에는 "맵기 정보 100% 결측", "정식·뷔페류 알레르기 `[]` 오태깅", "번역 왜곡",
"요금표 라벨 혼재" 같은 문제가 이미 서술되어 있었지만, 실제로 어느 정도 규모이고
어떤 패턴으로 나타나는지는 검증되지 않은 상태였습니다. 그래서 코드를 먼저 작성하지
않고, `data/labels/` 아래 실제 원본 JSON 1,000개 파일(13,980개 메뉴 어노테이션)을
탐색 에이전트로 전수 분석하는 것을 첫 단계로 삼았습니다. 이유는 두 가지입니다.

- 정의서의 서술이 맞는지 **수치로 확인**하지 않으면, 정제 로직의 임계값(예: "얼마나
  결측이면 결측으로 볼 것인가")을 임의로 정하게 되어 근거가 약해집니다.
- 정제 로직(특히 알레르기 신뢰도 판정, 번역 오류 판정)은 데이터의 실제 분포를 모르면
  과도하게 관대하거나 과도하게 공격적으로 작동할 위험이 있습니다.

분석 결과 다음이 확인되었습니다.

| 항목 | 확인된 수치 |
| --- | --- |
| `spicy_level` 결측률 | 98.3% (null) |
| 재료 있음 + 알레르기 `[]` (오태깅 의심) | 재료 보유 항목의 38.4% |
| 요금표/좌석단가가 메뉴로 오태깅 | 뷔페(BF01) 매장 다수에서 발견 (예: "초등학생", "성인") |
| 번역 오류 | 반복되는 패턴 확인 (예: `염통꼬치`(소염통) → en "Chicken Heart Skewers"가 4개 파일에서 동일하게 반복) |

이 수치를 근거로 각 정제 로직의 판정 기준을 설계했습니다. 즉, 코드가 먼저 있고
데이터에 끼워 맞춘 것이 아니라, 데이터 분석 결과가 코드 설계를 결정했습니다.

---

## 2. 무엇을 만들었는가

```
data/labels/**/*.json   (원본, 변경하지 않음)
        │
        ▼  preprocess.py   ─ 빌드타임 정제
data/processed/safemeal_pure.db
  ├─ menus            (정제된 메뉴 + 무결성 비트 플래그)
  ├─ menu_allergies    (24개 법정 알레르기 카테고리로 정규화)
  ├─ menus_fts          (FTS5, menu_id UNINDEXED)
  └─ excluded_rows        (격리된 비-메뉴 행 감사 로그)
        │
        ▼  FastAPI  ─ 런타임 필터 + 교정
/api/v1/menus/search  (지역/알레르기 회피/검색어)
        │
        ▼
app/static/index.html  (Vanilla JS 단일 정적 파일)
```

### 2-1. 왜 이렇게 나눴는가 (빌드타임 vs 런타임)

정의서에 "Pre-build 단계"와 "Runtime 단계"가 구분되어 있었기 때문에, 저도 로직을
두 단계로 나눴습니다.

- **빌드타임(`preprocess.py`)**: 무결성 플래그(`is_spicy_known`,
  `is_allergy_reliable`, `is_translation_error`)를 계산해 DB 컬럼으로 저장합니다.
  매 요청마다 다시 계산하면 낭비이고, 검색 시 `WHERE is_allergy_reliable = 0`처럼
  인덱스를 태운 필터링이 가능해야 하기 때문입니다.
- **런타임(`app/sanitizer.py`의 `DataSanitizer.clean_translation`)**: 실제 텍스트
  교정(예: 오역 필드를 "{로마자 표기} ({재료 목록})"로 치환)은 검색 결과로 반환되는
  20~100건에 대해서만 요청 시점에 수행합니다. 이렇게 하면 정제 로직을 고칠 때마다
  DB를 통째로 재생성하지 않아도 되고, `preprocess.py`와 `app/sanitizer.py`가
  `app/lexicon.py`라는 같은 키워드 사전을 참조하도록 만들어 두 단계가 서로 다른
  기준으로 판정하는 것(=로직 drift)을 방지했습니다.

### 2-2. 알레르기 신뢰도 판정을 만들다가 발견한 문제와 수정

처음에는 정의서 문구("정식·뷔페류의 알레르기 성분 `[]` 오태깅")를 그대로 따라
"재료(`ingredients.ko`)는 있는데 알레르기(`allergy`)가 비어 있으면 미신뢰"로만
판정했습니다. 그런데 실제 검색 테스트 중 `나시고랭(새우/삼겹살/소고기)`라는
항목이 발견됐습니다. 이름에 새우·삼겹살·소고기가 명시돼 있는데도 `ingredients.ko`와
`allergy`가 **둘 다** 비어 있어서, 원래 로직대로면 "신뢰할 수 있음(안전)"으로
분류되어 돼지고기 알레르기 회피 검색에서도 걸러지지 않는 상황이었습니다.

이건 정의서가 명시한 패턴(재료는 있는데 알레르기만 빠진 경우)보다 더 위험한
경우였습니다. 그래서 판정을 다음과 같이 확장했습니다.

- `mistagged_with_ingredients` (5,048건): 재료는 있는데 알레르기가 빔 → 정의서가
  지목한 원래 버그
- `no_data` (812건): 재료도 알레르기도 전무 → 검증 근거 자체가 없음

두 경우 모두 `is_allergy_reliable = 0`으로 통일해 안전 필터에서 함께 제외되도록
했습니다. "안전 검색"이라는 서비스 목적상, "정보가 없다"를 "안전하다"로 착각하게
만드는 것이 가장 치명적인 실패라고 판단했기 때문입니다. 이 부분은 정의서에 없던
설계 확장이라 별도로 보고드립니다.

### 2-3. 나머지 구현 요약

- `app/lexicon.py`: 지역코드/업종코드 라벨, 요금표 판별 정규식, 단백질 키워드
  사전(돼지/소/닭/새우/오징어 각각 ko/en/ja/zh_CN/zh_TW), 24개 법정 알레르기
  정규화 테이블. 세 곳(전처리, 런타임 교정, 향후 확장)이 공유하는 단일 소스로
  설계했습니다.
- `app/sanitizer.py`: `DataSanitizer.detect_translation_errors` (한국어 재료가
  암시하는 단백질과 번역문이 지칭하는 단백질이 다르면 플래그) /
  `clean_translation` (플레이스홀더 누출 제거 + 단백질 불일치 시 안전한 문구로 대체)
- `app/routers/menus.py`: `/api/v1/menus/search`가 지역 필터 → FTS5 검색 →
  `avoid_allergies` 지정 시 해당 알레르기 포함 항목과 `is_allergy_reliable=0` 항목을
  모두 드랍 → 남은 결과에만 `DataSanitizer.clean_translation` 적용, 순서로 동작
- `app/static/index.html`: 빌드 스텝 없는 단일 정적 파일. Tailwind CDN 대신
  인라인 CSS를 직접 작성했는데, 이는 "완전 오프라인 구동"이라는 정의서 요구사항과
  CDN 의존성이 충돌한다고 판단했기 때문입니다(정의서 표에는 Tailwind라고
  적혀 있었지만, CDN을 쓰면 인터넷 차단 환경에서 스타일이 깨집니다).
- `Dockerfile`: `python:3.11-slim` 기반, 이미지 빌드 시점에 `preprocess.py`를
  실행해 DB를 미리 구워 넣습니다. 컨테이너 기동 후에는 네트워크가 전혀 필요
  없습니다.

---

## 3. 무엇을 검증했고, 무엇을 검증하지 못했는가

### 검증한 것

- `preprocess.py` 실제 실행 → 1,000개 파일, 13,965개 메뉴 정상 적재, 통계 수치
  확인
- FTS5 검색, 알레르기 정규화(`굴`→`조개류` 등), 번역 오류 자동 검출 결과를 DB에서
  직접 조회해 확인
- `uvicorn`으로 서버를 실제로 띄워 다음을 curl/urllib로 호출해 응답 확인:
  `/api/v1/menus/search`(한국어 검색어 포함), `/api/v1/regions`,
  `/api/v1/store-types`, `/api/v1/allergens`, `/api/v1/stats`,
  `/api/v1/excluded-sample`, `/docs`, `/openapi.json`
- `avoid_allergies=돼지고기` 설정 시 나시고랭 항목이 실제로 검색 결과에서
  제외되는 것을 요청/응답으로 직접 확인 (`dropped_for_allergy_safety` 값 증가)
- 프론트엔드에 내장된 JS를 추출해 `node --check`로 문법 오류 여부 확인
- (참고) 로컬 curl이 한글 인자를 CP949로 잘못 인코딩하는 이슈를 발견 →
  Python `urllib`로 우회 검증. 이는 제 코드의 버그가 아니라 이 Windows 환경의
  curl 설정 문제였습니다.

### 검증하지 못한 것 (한계)

- **실제 브라우저로 열어보지 못했습니다.** 이 환경에는 브라우저 자동화/스크린샷
  도구가 없어서, HTML/JS가 문법적으로 옳고 API 응답 스키마가 프론트가 기대하는
  필드와 일치하는지까지만 확인했습니다. 실제로 클릭했을 때 레이아웃이 깨지지
  않는지, 칩(chip) 토글이 실제로 리렌더링을 트리거하는지 등은 **눈으로 확인하지
  못했습니다.**
- **Docker 빌드를 실행해보지 못했습니다.** 이 로컬 환경에 Docker가 설치돼 있지
  않아 `Dockerfile`/`docker-compose.yml`은 스펙대로 작성만 했을 뿐, 실제
  `docker compose up --build`가 성공하는지는 확인하지 못했습니다.
- 자동화 테스트 코드(pytest 등)를 작성하지 않았습니다. 수동 스모크 테스트만
  했습니다.

---

## 4. 부족하다고 느끼는 부분

솔직하게 말씀드리면 다음 부분들이 아직 미흡합니다.

1. **번역 오류 탐지가 완전하지 않습니다.** 단백질 키워드(돼지/소/닭/새우/오징어)
   불일치만 탐지하는 휴리스틱이라, 이 다섯 카테고리에 해당하지 않는 오역(예:
   조리법이 틀리거나, 존재하지 않는 요리로 완전히 잘못 번역된 경우)은 잡아내지
   못합니다. 실제 전체 데이터 중 몇 %가 오역인지는 모르는 상태이고,
   `is_translation_error=1`로 잡힌 136건(약 1%)은 **탐지 가능한 하한선**이지
   전체 오역 비율이 아닙니다.
2. **요금표 판별 정규식이 오탐/누락 가능성을 완전히 배제하지 못합니다.**
   "성인", "아이" 같은 단어가 실제 요리 이름에 우연히 들어가는 경우(negative
   lookahead로 일부만 방어) 잘못 제외될 수 있고, 반대로 제가 못 짐작한 패턴의
   요금표 행은 그대로 메뉴로 남아 있을 수 있습니다.
3. **알레르기 정규화 사전(`ALLERGEN_CATEGORY_MAP`)이 수작업 기반입니다.** 원본
   데이터의 모든 `allergy` 원시 토큰을 전수조사해서 만든 것이 아니라, 샘플링한
   패턴을 기반으로 만들었기 때문에, 사전에 없는 표기(동의어, 오타 등)는 정규화
   없이 원문 그대로 카테고리가 되어 회피 필터에서 그룹화되지 않을 수 있습니다.
4. **성능/스케일 테스트를 하지 않았습니다.** 14,000행 규모에서는 문제없이
   동작하지만, 데이터가 훨씬 커질 경우 현재의 "후보 ID를 파이썬 메모리로 모아
   페이지네이션" 방식은 SQL 레벨 LIMIT/OFFSET보다 비효율적입니다.
5. **보안/운영 고려가 거의 없습니다.** 인증, Rate limiting, CORS 정책 등은
   데모 목적상 구현하지 않았습니다. 실제 서비스로 노출하려면 추가 작업이
   필요합니다.
6. **원본 이미지·OCR 좌표(`boundary_coordinates`, `ocr.x/y`) 데이터를 전혀
   활용하지 않았습니다.** 정의서에 이미지 활용 요구가 없었고 원본 이미지
   파일도 제공되지 않아 텍스트 정보만 사용했는데, 실제 서비스라면 "메뉴판
   사진에서 이 항목이 어디 있는지" 같은 기능이 유용할 수 있습니다.
7. **UI가 최소 기능 수준입니다.** 반응형/접근성(a11y) 대응이 약하고, 모바일
   환경에서 카드 레이아웃이 어떻게 보이는지 확인하지 못했습니다.

---

## 5. 다음에 하면 좋을 것

- Docker 환경이 준비되면 `docker compose up --build` 1회 실행해 이미지 빌드가
  실제로 성공하는지 확인
- 실제 브라우저(또는 이 저장소를 로컬에서 여는 사람)로 UI를 열어 필터
  조합/페이지네이션/언어 토글을 직접 눌러보며 확인
- 번역 오류 탐지 범위를 단백질 5종 외로 확장하려면, 오역 샘플을 더 모아 패턴을
  추가하거나 편집거리(edit distance) 기반의 언어 간 길이/구조 이상탐지 같은
  보조 규칙을 추가하는 것을 검토
- `ALLERGEN_CATEGORY_MAP`을 원본 `allergy` 토큰 전수 집계 후 커버리지 100%로
  보강

---

*이 보고서는 대화 중 실제로 수행한 작업과 그 근거, 그리고 확인 가능/불가능했던
범위를 최대한 정직하게 정리한 것입니다.*
